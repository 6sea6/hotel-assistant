const { getVersionTag } = require('./shared');

function buildOptimizePrompt() {
  return `# 宾馆比较助手 AI 优化提示词 ${getVersionTag()}

## 优化目标
1. 降低启动等待感
2. 减少删除、新增、筛选、切换视图时的卡顿
3. 重点修复行式批量删除后再次添加宾馆无法立即输入的问题
4. 抽离重复逻辑和大段静态文案
5. 保持数据结构对 AI 友好
6. 保持 F12 开发者工具尽量使用中文，便于调试

## 优先级建议
### 一类：直接影响用户体感
- 弹窗打开后无法立即输入
- 删除操作导致主界面长时间无响应
- 行式批量删除后再次添加宾馆出现长时间无法输入
- 列表重渲染频繁造成明显卡顿
- 启动时同步读取过多内容

### 二类：结构与维护性
- 抽离大段默认文案、常量、配置
- 收敛重复的按钮确认逻辑、模态框逻辑、通知逻辑
- 减少超长文件中的无业务价值内容
- 让提示词与真实存储 schema 保持一致，避免遗漏 website、address、subway_station、subway_distance、bus_route、original_room_type、room_area、notes 等可填写字段
- 导出排名图片时避免文字重叠，不显示 address 与 bus_route；总价格与日均价格应合并为同一信息格上下展示，其余字段按每行三个信息格重排，最近地铁站以"距地铁"显示且只展示 subway_distance
- 统一 subway_distance 为 0 时显示无较近地铁站的展示逻辑，避免不同视图表现不一致
- 卡片视图中的 website 应显示在 address 右侧独立信息列，address 固定在左侧并保留可读宽度，website 从右侧信息列的固定起点开始显示，继续保持单行省略与原始链接跳转
- 卡片视图要在宾馆名称下展示 original_room_type，行式视图不要展示地址和公交路线，只在名称下显示原始房型名

### 三类：构建与体积
- 打包脚本避免每次全量安装依赖
- 优先使用已有锁文件做稳定安装
- 保留必要构建产物，避免把无关资源打进包内

## 执行要求
- 先确认根因，再改代码。
- 采用最小可行改动，不做无关重写。
- 每次改动后验证主要流程：添加、编辑、删除、收藏、模板、设置、提示词、导入导出。
- F12 打开的开发者工具应尽量保持中文，方便定位渲染与 IPC 问题。
- 如果存在无法完全实现的外部限制，要明确说明边界。`;
}

module.exports = {
  buildOptimizePrompt
};
